export const V104_DIRECTION_WEIGHTS = Object.freeze({
  roadmap_trend_signals: 0.275,
  ask_road_signals: 0.275,
  shoe_banker_player_bias: 0.35,
  neutral_reserve: 0.10,
})

export const V104_SHOE_BIAS = Object.freeze({
  priorSampleSize: 8,
  maximumEdge: 0.08,
})
