# v096 主副預測信心比例調整

- 正式策略：`v096_副預測權重與信心校準版`；proxy 服務版本：`096`。
- 主預測方向、主權重與出手規則不變。原始信心仍採方向一致度 70% 加訊號強度 30%，`原始 = 30 + 40 × 綜合強度`；最終信心採 `45 + 2 × (原始 - 32.03)`，四捨五入並限制於 30～70。
- 副預測不增刪權重項，只調整既有項目比例；門檻為和局 47、超六 65、莊對 53、閒對 55、莊龍 53、閒龍 57。
- 前端主預測僅顯示後端 `prediction`，不使用桌況重算主方向或信心值，UI 版面不變。
- 回歸測試覆蓋六組副預測權重總和、各門檻、主信心換算與上下限，以及 API 策略／服務版本。
