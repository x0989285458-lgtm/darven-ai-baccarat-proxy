MT_LOGIN_URL=https://mt.example.invalid/login?token=REPLACE_ON_VM
NODE_ENV=production
WORKER_ADMIN_KEY=REPLACE_ON_VM
PUSH_TARGET_URL=https://your-render-service.example/api/cloud-ingest/snapshot
INGEST_KEY=REPLACE_ON_VM
PUSH_INTERVAL_MS=5000
PUSH_QUEUE_PATH=/app/data/latest-snapshot.json
PORTAL_CREDENTIALS_FILE=/run/secrets/portal-credentials.json
MT_SESSION_PATH=/app/data/mt-session.json
MT_HOST_ALLOWLIST=
PORTAL_REFRESH_BACKOFF_MS=5000
HEADLESS=true
PORT=8787
